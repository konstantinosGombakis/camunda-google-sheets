version: "3.8"

services:

  camunda:
    image: camunda-with-authentication
    build: camunda-with-authentication
    container_name: camunda-bpm
    ports:
      - "8080:8080" # http://localhost:8080/camunda-welcome/index.html
    networks:
      - ed-network

networks:
  ed-network:
